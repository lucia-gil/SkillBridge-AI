package com.nexacorp.skillbridge.model;

/** Valores válidos para `notificaciones.canal` (distinto del set de `preferencias_notificacion.canal`, ver {@link CanalPreferencia}). */
public enum CanalNotificacion {
    app,
    mail,
    app_mail
}
